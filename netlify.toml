[build]
  command = "npm install"
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Express 한 덩어리로 서비스 (serverless-http)
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
  force = true
